#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*FILE *fp;

    char buffer[100] = "Esto es un texto dentro del fichero.";
    char bufferIn[100]

    fp = fopen( "D:\\clase\\fichero.txt","w" );

    fprintf(fp,buffer);

    fclose( fp );
    system("pause");
    system("cls");*/

    return 0;
}
